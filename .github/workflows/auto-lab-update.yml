name: Auto publish LAB update

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'FIRE_DIRECTION_LAB_v*.html'

permissions:
  contents: write

concurrency:
  group: fire-direction-lab-update
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update update.json from newest LAB HTML
        shell: python
        run: |
          from pathlib import Path
          import hashlib, json, re

          root = Path(".")
          rx = re.compile(
              r"^FIRE_DIRECTION_LAB_v(\d+)\.(\d+)\.(\d+)\.(\d+).*\.html$",
              re.I
          )

          found = []
          for p in root.glob("FIRE_DIRECTION_LAB_v*.html"):
              m = rx.match(p.name)
              if not m:
                  continue

              version_tuple = tuple(map(int, m.groups()))
              found.append((version_tuple, p))

          if not found:
              raise SystemExit("FIRE_DIRECTION_LAB_v*.html 파일을 찾지 못했습니다.")

          (major, minor, patch, rev), game = max(found, key=lambda x: x[0])

          # 기존 LAB build 규칙:
          # 2.30.7.124 -> 23007124
          # 2.30.8.0   -> 23008000
          build = (
              major * 10_000_000
              + minor * 100_000
              + patch * 1_000
              + rev
          )

          version = f"{major}.{minor}.{patch}.{rev}"
          sha256 = hashlib.sha256(game.read_bytes()).hexdigest()

          data = {
              "version": version,
              "build": build,
              "file": game.name,
              "sha256": sha256,
              "notes": f"FIRE DIRECTION LAB v{version}",
              "mandatory": False
          }

          Path("update.json").write_text(
              json.dumps(data, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8"
          )

          print(f"version = {version}")
          print(f"build   = {build}")
          print(f"file    = {game.name}")
          print(f"sha256  = {sha256}")

      - name: Commit update.json
        shell: bash
        run: |
          if git diff --quiet -- update.json; then
            echo "update.json already current"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add update.json
          git commit -m "chore: publish latest LAB update"
          git push
